# Frontend Environment Variables

# WebSocket Host (optional - defaults to current host or localhost:8000 in development)
# VITE_WS_HOST=localhost:8000

# API Base URL (optional - defaults to /api)
# VITE_API_BASE_URL=/api
